Flask==3.0.0
python-dotenv==1.0.0
supabase==2.4.2
httpx==0.25.2
gotrue==2.8.0
Werkzeug==3.0.1
python-dateutil==2.8.2
gunicorn==20.1.0
bcrypt==4.1.1
# Removed explicit httpcore pin to avoid conflict with httpx dependency resolver.
# httpx will bring a compatible httpcore version automatically.

setuptools==82.0.0
wheel>=0.40.0

# psycopg2-binary removed: not required by this app (Supabase client uses HTTP)
# Removing avoids build failures on platforms without libpq/pg_config (eg. Vercel)

# Scheduler for background reminder jobs
APScheduler==3.10.1
